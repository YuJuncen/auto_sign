# 能签上到吗？

这个测试来测试我们的签到工具能不能正常运行。    
说不定能呢？  

## 应该可以登陆获取 token, 而 token 应该是一个 70 位的字符串。

* We can get a token if we input right username and password.

## 我们的工具在执行函数之后，我们应该会签上到。
在进行这一步之前，还请确定现在在 21:00~23:30 之间。
* after sign, we will get no sign infomation.